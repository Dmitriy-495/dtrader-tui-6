package ui

// =============================================================================
// Settings — компонент шестерёнки в правом углу header
// Пока только иконка, позже — модалка настроек (Ctrl+,)
// =============================================================================

// renderSettings — иконка шестерёнки
// TODO: обработка Ctrl+, → открытие модалки настроек
func (m Model) renderSettings() string {
	return OrangeStyle.Render("⚙")
}
